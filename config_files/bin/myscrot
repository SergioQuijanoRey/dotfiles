#!/usr/bin/env bash

# The destination file
mkdir -p "$HOME/Scrots/"
dest_file="$HOME/Scrots/$(date +'%d-%m-%Y::%H:%M:%S').png"
echo "$dest_file"

grim -g "$(slurp)" - | wl-copy && wl-paste > $dest_file | dunstify "Screenshot of the region taken" -t 1000
